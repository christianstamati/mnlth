{
  "name": "mnlth",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "down": "docker compose -f docker/docker-compose.yml down",
    "reset": "docker compose -f docker/docker-compose.yml down -v",
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "check:ci": "biome ci .",
    "typecheck": "tsc --noEmit && turbo typecheck",
    "test": "bun test scripts packages/backend/clone && turbo test",
    "reset:aws": "bash scripts/reset-aws.sh",
    "bake:ami": "bash scripts/bake-ami.sh",
    "convex:deploy": "bun scripts/convex-deploy.ts",
    "convex:clone": "bun scripts/convex-clone.ts"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.9",
    "@types/bun": "^1.4.0",
    "@types/node": "^26.4.0",
    "sst": "^4.17.1",
    "turbo": "^2.9.18",
    "typescript": "^6"
  },
  "packageManager": "bun@1.4.0",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "lightningcss"
    ]
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
