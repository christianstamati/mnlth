node_modules
.DS_Store
dist
dist-ssr
*.local
.env*
.tanstack
.wrangler
.output
.vinxi
__unconfig*
todos.json

# turbo
.turbo

# typescript
*.tsbuildinfo

# sst
.sst
sst-env.d.ts

# the RDS certificate bundle is fetched at setup, not committed
certs/*.pem

# ...but the example env file is meant to be read
!.env.example

# playwright
playwright-report
test-results
