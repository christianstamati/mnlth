{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalPassThroughEnv": ["VITE_CONVEX_URL"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".output/**"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "setup-dev": {
      "cache": false
    },
    "dev": {
      "dependsOn": ["@workspace/backend#setup-dev"],
      "cache": false,
      "persistent": true
    }
  }
}
