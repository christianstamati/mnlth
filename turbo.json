{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "globalPassThroughEnv": ["VITE_CONVEX_URL", "VITE_STAGE_NAME"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".output/**"]
    },
    "typecheck": {
      "dependsOn": ["^typecheck"]
    },
    "up": {
      "cache": false
    },
    "dev": {
      "dependsOn": ["@workspace/backend#up"],
      "cache": false,
      "persistent": true
    }
  }
}
