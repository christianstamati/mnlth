---
"web": minor
---

Deploys run from GitHub Actions again: branch-to-stage mapping moves to `deployments.json`, the settings file folds into `sst.config.ts`, and the build footer, Sentry client and monitoring go away.
